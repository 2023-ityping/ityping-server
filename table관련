CREATE TABLE users (
  email VARCHAR(100) NOT NULL,
  pw VARCHAR(100),
  nickname VARCHAR(20) NOT NULL,
  stu_emmat INT,
  stu_shortcut INT,
  pra_emmat INT,
  pra_shortcut INT,
  PRIMARY KEY (email)
);

create table game(
  email VARCHAR(100) NOT NULL,
  score INT,
  gametype varchar(30)
)